#	$NetBSD: dot.shrc,v 1.3 2007/11/24 11:14:42 pavel Exp $

if [ -f /etc/shrc ]; then
	. /etc/shrc
fi

case "$-" in *i*)
	# interactive mode settings go here

	PLAN9=/home/x/dl/plan9port-master export PLAN9
	PATH=$PATH:$PLAN9/bin export PATH

	PS1="$(pwd | sed "s,^$HOME,~,") > "
	set -o emacs
	;;
esac
